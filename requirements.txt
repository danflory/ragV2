# --- CORE FRAMEWORK ---
fastapi>=0.109.0
uvicorn>=0.27.0
python-dotenv>=1.0.0
pydantic>=2.11.0
pydantic-settings>=2.5.2
mcp>=1.25.0
click>=8.1.0

# --- NETWORK & API ---
httpx>=0.27.0
requests>=2.31.0
python-multipart>=0.0.9

# --- LLM DRIVERS ---
# Google Gemini (L3)
google-genai>=0.2.0
google-api-python-client>=2.100.0
google-auth>=2.20.0

# Ollama (L1)
ollama>=0.1.0

# --- MEMORY & AI (THE FIX) ---
# CRITICAL: Force Numpy 1.x to prevent "RuntimeError" or "__version__" crashes
numpy<2.0.0

# PyTorch (Let it pull its own nvidia deps)
torch>=2.2.0 --index-url https://download.pytorch.org/whl/cu121

# Vector DB & Embeddings
chromadb>=0.4.24
sentence-transformers>=2.6.0
transformers>=4.39.0
huggingface-hub>=0.22.0
tokenizers>=0.15.0

# Gravitas Grounded Research (Hybrid Search)
qdrant-client>=1.7.0
minio>=7.2.0
FlagEmbedding>=1.2.0

# Database (Postgres)
asyncpg>=0.29.0

# --- DEV TOOLS & TESTING ---
pytest>=8.0.0
pytest-asyncio>=0.23.0
ruff>=0.3.0
black>=24.2.0
coloredlogs>=15.0.0
