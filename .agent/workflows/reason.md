---
description: Run the Antigravity Reasoning Pipe to handle session transitions and archival.
---

1. Execute the reasoning pipe script to archive the current session buffer and reset for the new session.

// turbo
2. Run the script:
```bash
python3 /home/dflory/dev_env/Gravitas/ANTIGRAVITY_Scripts/reasoning_pipe.py --init
```
